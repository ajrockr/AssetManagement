{% extends 'base.html.twig' %}

{% block body %}
	<div class="container">
		<div class="card">
			<div class="card-header">
				<div class="row">
					<div class="col text-start">
						<h4>New repair</h6>
					</div>
					<div class="col text-end">
						<a href="{{ path('app_repair_index') }}" role="button" class="btn btn-sm">
							<i class="fa-solid fa-x"></i>
						</a>
					</div>
				</div>
			</div>
			<div class="card-body">
                {{ include('repair/_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}


