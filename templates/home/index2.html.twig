{% extends 'base.html.twig' %}

{% block body %}
    <div class="card" style="width:500px;">
        <div class="card-header text-justify">
            <div class="row w-100">
            <div class="col-md-9">Asset Counts</div>
            <div class="col-md-3 text-end"><a href="#" class="text-decoration-none"><i class="fa-solid fa-file-lines"></i> Report</a></div>
            </div>
        </div>
        <div class="card-body">
            {{ render_chart(assetsTotalChart) }}
        </div>
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('chartjs') }}
    <script type="text/javascript">
        const myChart = new Chart();
    </script>
{% endblock %}
