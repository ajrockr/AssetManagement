{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="card">
            <div class="card-header text-end">
                <a href="{{ path('app_asset_index') }}" role="button" class="btn btn-sm">
                    <i class="fa-solid fa-x"></i>
                </a>
            </div>
            <div class="card-body">
                {{ include('asset/_form.html.twig') }}
            </div>
        </div>
    </div>

{% endblock %}
