<form method="post" action="{{ path('app_asset_storage_delete', {'id': asset_storage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ asset_storage.id) }}">
    <button class="btn">Delete</button>
</form>
