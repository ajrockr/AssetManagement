{% extends 'base.html.twig' %}

{% block title %}RepairParts{% endblock %}

{% block body %}
    <h1>RepairParts</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ repair_part.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ repair_part.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ repair_part.description }}</td>
            </tr>
            <tr>
                <th>Cost</th>
                <td>{{ repair_part.cost }}</td>
            </tr>
            <tr>
                <th>Vendor</th>
                <td>{{ repair_part.vendor }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_repair_parts_index') }}">back to list</a>

    <a href="{{ path('app_repair_parts_edit', {'id': repair_part.id}) }}">edit</a>

    {{ include('repair_parts/_delete_form.html.twig') }}
{% endblock %}
